server {
    listen 80;
    server_name nguyen-huy-hoang-k23.rikkeieducation.com;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri /index.html;
    }

    location /api/ {
        proxy_pass http://backend:8080;
    }
}